spring:
  application:
    name: membership
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: 5352
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: postgresql
